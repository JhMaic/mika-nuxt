export default defineNuxtRouteMiddleware((to, from) => {
  //
  // const app = useNuxtApp();
  // alert(`login: ${app.$liff.isLoggedIn()}`);
  // if (!app.$liff.isLoggedIn()) {
  //   if (to.query.isLogin === "0") return;
  //   return navigateTo({ path: "", query: { isLogin: 0 } });
  // }
});
